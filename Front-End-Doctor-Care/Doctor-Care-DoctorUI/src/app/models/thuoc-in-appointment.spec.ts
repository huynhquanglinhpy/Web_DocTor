import { ThuocInAppointment } from './thuoc-in-appointment';

describe('ThuocInAppointment', () => {
  it('should create an instance', () => {
    expect(new ThuocInAppointment()).toBeTruthy();
  });
});
