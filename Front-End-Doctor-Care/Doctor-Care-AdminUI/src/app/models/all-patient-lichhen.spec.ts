import { AllPatientLichhen } from './all-patient-lichhen';

describe('AllPatientLichhen', () => {
  it('should create an instance', () => {
    expect(new AllPatientLichhen()).toBeTruthy();
  });
});
