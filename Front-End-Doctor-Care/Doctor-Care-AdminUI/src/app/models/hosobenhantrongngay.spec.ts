import { Hosobenhantrongngay } from './hosobenhantrongngay';

describe('Hosobenhantrongngay', () => {
  it('should create an instance', () => {
    expect(new Hosobenhantrongngay()).toBeTruthy();
  });
});
